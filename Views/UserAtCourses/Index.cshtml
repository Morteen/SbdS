@model IEnumerable<SbdS.Models.UserAtCourse>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Course.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Student.Fname)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsPayed)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Course.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Student.Fname)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsPayed)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AtCourseId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AtCourseId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AtCourseId })
        </td>
    </tr>
}

</table>
